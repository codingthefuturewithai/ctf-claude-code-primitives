[project]
name = "ctfai-brand"
version = "0.2.0"
description = "CTFAI brand assets and template-based PDF generation"
requires-python = ">=3.9"
dependencies = [
    "jinja2>=3.0",
    "weasyprint>=60.0",
    "pyyaml>=6.0",
]
